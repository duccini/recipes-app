import { StyleSheet } from "react-native";
import colors from "../../styles/colors";

const styles = StyleSheet.create({
  title: {
    fontSize: 16,
    fontWeight: "bold",
    color: colors.black,
  },
});

// named exports: export { styles }
// default exports: export default styles

export default styles;
